{
  "build": {
    "install": {
      "cmds": ["/app/.venv/bin/pip install -r requirements.txt"]
    }
  },
  "deploy": {
    "startCommand": "PYTHONPATH=/app/src python -m uvicorn aiplatform.webapp.main:app --host 0.0.0.0 --port $PORT"
  }
}
